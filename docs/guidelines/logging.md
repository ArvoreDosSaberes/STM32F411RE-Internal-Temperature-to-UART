# Diretrizes de logging

Neste projeto de demonstração, o "logging" é feito principalmente via
mensagens de texto na UART (terminal serial).

- Prefixar mensagens de depuração quando necessário (ex.: `DBG:`).
- Para medições de temperatura, manter um formato estável, fácil de
  processar por ferramentas externas.
